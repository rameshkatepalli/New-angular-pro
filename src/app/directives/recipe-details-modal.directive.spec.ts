import { RecipeDetailsModalDirective } from './recipe-details-modal.directive';

describe('RecipeDetailsModalDirective', () => {
  it('should create an instance', () => {
    const directive = new RecipeDetailsModalDirective();
    expect(directive).toBeTruthy();
  });
});
